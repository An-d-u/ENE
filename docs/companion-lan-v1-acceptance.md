# 동반 앱 조기 LAN 수용 확인

2026-09-17: 현재 대화·실제 ENE 접수/종료·Live2D·자동 음성 출력·쓰다듬기·공통 설정의 소스 구현과 자동 검증을 마쳤다. 실제 단말 선택/설치 확인은 보류되어 있다. 이 문서는 단말 인수 기준표이며 LAN 연결 성공 기록이 아니다.

PC와 별도 ENE_APP 저장소 모두 사용자 승인으로 작업 브랜치를 로컬 `main`에 병합했다. 최신 검증 커밋·시험 수·APK 해시는 [미디어 검증 기록](companion-media-validation.md)에 기록했다. Android의 `docs/media-support.md`와 `docs/build-and-install.md`도 함께 따른다. 원격 저장소에 게시하거나 APK를 설치하지 않았다.

| 항목 | 현재 결과 |
| --- | --- |
| Android 단위·정적 검사·개발/계측 APK 빌드 | 통과. 최신 수치는 미디어 검증 기록 참조; 단말 계측 실행은 제외 |
| 사용자 단말 선택/설치 동의 및 APK 설치 | 미검증·미실행 |
| 실카메라 QR·OS 권한 거절/재시도/설정 복구 | 미검증 |
| HTTPS/WSS QR → PC 명시 승인 → 가상 메시지 왕복 | 미검증 |
| 실제 전체 대화 표시·응답 계수 1회 증가 | 미검증 |
| 전경 복귀·회전·절전·단절 복구·초안/중복 방지 | 미검증 |
| 실제 Keystore·프로세스 재생성·백업 제외 | 미검증 |
| 인증서 불일치 차단·QR 만료·승인 거절·주소 후보 | 실제 단말 미검증 |
| 실제 ENE AI 접수/공개 대화/TTS/트레이/종료 연결 | 구현 및 실제 Qt/TLS·합성 제공자 자동 검증 완료; 실제 폰·유료 제공자 인수는 미실시 |
| Live2D·립싱크·쓰다듬기·공통 설정 | 소스·계약·수명 자동 검증 완료; 실제 렌더링·소리·터치 미검증 |

시험은 `.venv\Scripts\python.exe -m tools.companion_smoke`의 합성 응답으로 시작한다. 이 도구는 임시 등록을 사용하므로 종료 후 재실행은 새 QR이 필요할 수 있다. 실제 대화·API 키·단말 일련번호·QR·토큰은 결과 자료로 남기지 않는다.

단말 접근이 없으면 Task 6을 성공 처리하지 않는다. 사용자와 시험 대상을 정한 뒤 설치하고, 같은 개인 LAN·카메라·왕복을 먼저 확인한다. 방화벽 변경은 실행 파일/포트·개인 프로필·로컬 서브넷으로 범위를 정해 별도 확인을 받는다. 실제 ENE 미디어 인수에서는 사용할 모델·제공자와 비용을 먼저 정한다. 외부 AI 비용이 발생하는 시험, Tailscale 설치, 공개 릴리스는 자동으로 수행하지 않는다.
